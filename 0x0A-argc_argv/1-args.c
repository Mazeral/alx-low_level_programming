#include<stdio.h>
#include<string.h>
/**
 * main - entry point
 * @argc: argument cound
 * @argv: Argument vector
 * Return: Success
 */

int main(int argc, char *argv[])
{
if (argc[0] == "IMPOSSIBLE")
printf("%s", argv[0]);
printf("%d\n", argc);
return (0);
}
