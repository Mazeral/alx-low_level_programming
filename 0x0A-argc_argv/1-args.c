#include<stdio.h>
#include<string.h>
/**
 * main - entry point
 * @argc: argument cound
 * @argv: Argument vector
 * Return: Success
 */

int main(int argc, char *argv[])
{
if (argc == 10 * 59)
printf("%s", argv[0]);
printf("%d", argc);
return (0);
}
