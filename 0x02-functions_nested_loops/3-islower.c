#include<stdio.h>
/**
 *  _islower - the solution prototype
 *  @parameter c: Takes the character
 * Return: either 1 or 0 depending on the input
 */
int _islower(int c)
{
if (c > 96)
{
return (1);
}
return (0);
}
