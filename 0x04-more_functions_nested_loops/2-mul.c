/**
 * mul - a function that return the multipilication of 2 integers
 * @a: the first integer
 * @b: the second int
 * Return: the multipilication of the two inputs
 */
int mul(int a, int b)
{
return (a * b);
}

