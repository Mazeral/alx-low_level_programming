#include"main.h"
#include<math.h>
#include<string.h>
#include<stdio.h>
/**
 * _abs - the function of the solution
 * @i: the input
 * Return: absolute value of the input
 */
int _abs(int i)
{
if (i > 0)
return (i);
else if (i < 0)
return (-i);
else
return (0);
}
